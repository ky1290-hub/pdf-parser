interface:
  display_name: "PDF Parser"
  short_description: "Extract text, OCR, tables, and PDF layout"
  default_prompt: "Use $pdf-parser to parse this authorized PDF with my LlamaCloud key into structured Markdown and JSON."
